#include <iostream>
using namespace std;

enum class Color{red, blue, green};
enum class Traffic_light{green, yellow, red};

int main() {

    Color color = Color::red;
    Traffic_light t_light = Traffic_light::red;
}